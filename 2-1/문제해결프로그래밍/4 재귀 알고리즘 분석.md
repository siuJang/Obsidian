---
tags:
  - DAY4
---

```
// 입력한 숫자가 역순으로 출력되는 재귀함수
void printup(int n) {
    if (n > 0) {
        printup(n - 1);
        printf("%d\n", n);
    }
}

int main()
{
    printup(4);
    return 0;
}
```
